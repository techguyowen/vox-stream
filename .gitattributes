# Auto-detect text files and perform LF normalization for source code
* text=auto

# Windows batch files MUST use CRLF line endings or cmd.exe will misseek on labels/goto
*.bat text eol=crlf
*.cmd text eol=crlf
